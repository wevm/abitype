{
  // This configuration is used for local development and type checking.
  "extends": "./tsconfig.base.json",
  "include": [".scripts/**/*.ts", "examples", "src", "test", "vite.config.ts"],
  "exclude": [],
  "compilerOptions": {
    "types": ["bun-types"]
  }
}
